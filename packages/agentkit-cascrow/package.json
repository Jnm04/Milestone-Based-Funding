{
  "name": "agentkit-cascrow",
  "version": "1.0.0",
  "description": "Coinbase AgentKit action provider for Cascrow — agentic escrow and milestone verification on the XRP Ledger",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "keywords": ["agentkit", "coinbase", "cascrow", "escrow", "xrpl", "rlusd", "action-provider"],
  "license": "MIT",
  "peerDependencies": {
    "@coinbase/agentkit": ">=0.4.0"
  },
  "devDependencies": {
    "@coinbase/agentkit": "^0.4.0",
    "typescript": "^5.0.0",
    "zod": "^3.0.0"
  },
  "dependencies": {
    "zod": "^3.0.0"
  }
}
